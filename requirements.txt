# Arquithon agora usa apenas a biblioteca padrão do Python (tkinter, sqlite3,
# hashlib) para a aplicação em si. Nenhuma dependência de terceiros é
# necessária para rodar "python app.py".
#
# O PyInstaller só é necessário para gerar o executável (.exe):
pyinstaller>=6.0
